INCLUDES = $(GTK_CFLAGS) -Wall -ansi	\
	$(GNOME_INCLUDEDIR)

bin_PROGRAMS = gnome-edit

gnome_edit_SOURCES = 		\
	gnome-edit.c


gnome_edit_LDADD =             	\
	$(GNOME_LIBDIR) 	\
	$(GNOME_LIBS)		\
	$(GTK_LIBS)		\
	$(INTLLIBS)
